import Login from '../../_components/Forms/Login/Login';

export default function LoginPage() {
    return (
        <div>
            <h1>Login as registered User</h1>
            <Login></Login>
        </div>
    );
}